import { View, Text } from 'react-native';

const SnareScreen = () => {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Snare Screen</Text>
    </View>
  );
}

export default SnareScreen;
import { View, Text } from 'react-native';

const VirtualKeyboardScreen = () => {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Virtual Keyboard Screen</Text>
    </View>
  );
}

export default VirtualKeyboardScreen;